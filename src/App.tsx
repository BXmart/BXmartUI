import './App.css';
import { Button } from './components';

function App() {
  return (
    <main className='flex gap-2'>
      <Button isLoading>Botón</Button>
      <Button>Botón</Button>
    </main>
  );
}

export default App;
